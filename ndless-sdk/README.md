
## Apple Silicon Support
This version includes native build support for Apple Silicon (arm64) Macs. Ensure you have the native arm64 GNU ARM toolchain installed (e.g., via Homebrew or native build) to compile.
